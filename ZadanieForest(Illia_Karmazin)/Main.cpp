#include <iostream>

using namespace std;

#include "Tree.hpp"

int main()
{
	int menu_num;
	do
	{
		cout << "Menu :" << endl << "1. Add tree to forest" << endl << "Enter number : ";
		cin >> menu_num;
		switch (menu_num)
		{
		case 1 :

		default:
			cout << "Wrong number";
			break;
		}

	}while(true);
    return 0;
}